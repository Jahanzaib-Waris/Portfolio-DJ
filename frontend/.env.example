# Leave blank for local dev — Vite's dev-server proxy forwards /api to Django.
# In production, set this to your deployed backend Vercel project's API, e.g.
# https://your-api.vercel.app/api
VITE_API_BASE_URL=
