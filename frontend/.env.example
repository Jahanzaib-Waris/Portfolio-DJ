# Leave blank for local dev — Vite's dev-server proxy forwards /api to Django.
# In production (Vercel), set this to your deployed Render API, e.g.
# https://your-api.onrender.com/api
VITE_API_BASE_URL=
